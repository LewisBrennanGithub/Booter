const GamesPage = ({ route }) => {
  const { games, handleDeleteGame, handleJoinGame, handleUpdateGame, loggedPlayer, handleSetGameCompletedStatus } = route.params;

  return (
      <View>
          <Text>Hi</Text>
      </View>
  );
};
